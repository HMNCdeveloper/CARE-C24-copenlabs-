<Window x:Class="HS5.MessageComboBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HS5.Resources.Idiomas"  
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="" Height="159" Width="388" MaxHeight="159" MaxWidth="388">
    <StackPanel Margin="10">
        <TextBlock Text="{x:Static local:Lenguaje.optionMessage}" Margin="0,0,0,10" Width="244"/>
        <ComboBox x:Name="comboBoxOptions" Width="250" Margin="0,0,0,10"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{x:Static local:Lenguaje.btnAcept}" Width="75" Margin="0,0,5,0" Click="OkButton_Click"/>
            <Button Content="{x:Static local:Lenguaje.btnCancel}" Width="75" Click="CancelButton_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
